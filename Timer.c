
#include "Timer.h"

entrees *io;
int shmid;
int timer_depart_timer;

///////////////////////////////////////////////////////////////
void timer_raz()
{
io=acces_memoire(&shmid);
if (io==NULL) printf("Erreur pas de mem sh\n");
timer_depart_timer=io->timer_sec;
}
////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////
int timer_valeur()
{
return (io->timer_sec) - ( timer_depart_timer);
}
/////////////////////////////////////////////////////////////////

